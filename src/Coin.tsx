function Coin(){
    return (
        <div>
            <span>
                list
                <p>list.map((t) => t.map(t.name) )</p>
            </span>
        </div>
    );
}
export default Coin;